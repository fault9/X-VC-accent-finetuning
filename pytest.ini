[pytest]
testpaths = tests
markers =
    gpu: needs a CUDA GPU (excluded from the default CPU run; run with -m gpu)
    slow: long-running (excluded by default; run with -m slow)
addopts = -m "not gpu and not slow"
