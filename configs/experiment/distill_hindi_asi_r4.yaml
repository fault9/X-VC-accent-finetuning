# Rank-4 distillation arm: rank-limited delta on the working same-timeline
# distillation objective (teacher-rendered ASI pairs), AdaLN + prenet
# placement. See CHANGES.md "Interim result ... low-rank DISTILL arms".
# Legacy: configs/finetune_distill_hindi_asi_lora_r4_alpha16.yaml
defaults:
  - model/xvc_16k
  - adapter/lora_distill_stack
  - dataset/distill_hindi_asi
  - training/distill_short
  - _self_

model:
  generator:
    lora:
      r: 4
